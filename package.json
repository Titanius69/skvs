{
  "name": "skvs-server",
  "version": "0.2.0",
  "description": "Super KVS – SQLite‑style key‑value store with HTTP API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cargo napi build --release && cp target/release/skvs.node .",
    "test": "python test_skvs.py"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "ip-range-check": "^0.2.0",
    "toml": "^3.0.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.4.5"
  }
}